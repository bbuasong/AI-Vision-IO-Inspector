# 의사결정 기록

## ADR-001: 문서 구조 분리

- 날짜: 2026-05-29
- 상태: 적용
- 결정: 프로젝트 정보, 원본 자료, 요구사항, 설계, 개발 추적 문서를 폴더별로 분리한다.
- 이유: 메일, 문서, 음성 파일에서 나온 정보를 구현 작업으로 연결할 때 근거, 질문, 확정 사항을 구분하기 위해서다.

## ADR-002: 측정 항목은 측정부 목록 기반으로 설계

- 날짜: 2026-05-29
- 상태: 제안
- 결정: 길이, 너비, 높이, 두께 고정 컬럼만으로 설계하지 않고 부품별 측정부 목록 구조를 기본안으로 둔다.
- 이유: 업무파악 정리파일에서 고객이 이미지 위에 측정 부위를 선으로 지정하고 측정부, 측정부2, 측정부3 형태로 표시하는 방식을 제안했다.
- 확인 필요: 이 방식이 최종 요구사항인지 고객/PM 확인이 필요하다.

## ADR-003: 라벨/바코드 입력은 어댑터로 분리

- 날짜: 2026-05-29
- 상태: 제안
- 결정: 검사 대상 입력은 스캐너, QR/바코드, 카메라 OCR 방식이 확정되기 전까지 입력 어댑터로 분리한다.
- 이유: 입력 방식에 따라 Application 개발 범위가 달라지므로 핵심 검사 흐름과 입력 장치를 분리해야 한다.

## ADR-004: 원본 요청 처리 절차를 문서화

- 날짜: 2026-05-29
- 상태: 적용
- 결정: 신규 메일/전화/음성 요청은 `00-inbox/intake-workflow.md` 절차에 따라 접수, 요약, 반영한다.
- 이유: 고객 요청이 계속 추가될 가능성이 있어 요구사항 누락과 작업 범위 혼선을 줄이기 위해서다.

## ADR-005: 기준정보/이력 DB는 SQLite로 시작

- 날짜: 2026-05-29
- 상태: 적용
- 결정: 로컬 기준정보와 검사 이력 저장소는 SQLite `Tests/AI-Vision IO Inspector/DB/DataBase.db`로 관리한다.
- 이유: 현재 요구는 단일 WPF 검사 프로그램 중심이며, 서버 설치 없이 무료로 배포/백업 가능한 DB가 적합하다.
- 구조: 부품 기준정보는 `PartList_*`, 검사 이력은 `History_*` 테이블로 분리한다.
- 초기 데이터: `export_Test.csv`는 테스트용 1회성 적재 원본이며, 앱은 실행 중 이 파일을 찾거나 읽지 않는다.
- 확장: 여러 검사 장비가 중앙 DB를 공유해야 하는 요구가 생기면 `IPartRepository`, `IInspectionRepository` 구현체만 PostgreSQL 등 서버형 DB로 교체한다.
- 위치 변경: 개발 중 DB와 기준 이미지는 상위 프로젝트 루트가 아니라 실제 관리 중인 WPF 솔루션 폴더 `Tests/AI-Vision IO Inspector/DB` 아래에 둔다.

## ADR-006: 측정용 영상 취득은 직접 카메라 SDK를 우선한다

- 날짜: 2026-05-29
- 상태: 적용
- 결정: DC-T3145G/R 카메라의 검사 원본 프레임은 카메라 SDK로 직접 취득하고, NVR/RTSP는 녹화/모니터링 보조 경로로 둔다.
- 이유: VLAD Source의 RTSP 구현은 1920x1080 고정 처리와 프레임 메타데이터 부족이 있어 5M 4:3/6:5 측정용 원본으로 바로 쓰기 어렵다.
- 확정: 2026-05-30 사용자 결정에 따라 NVR은 측정 원본이 아니라 녹화/모니터링 보조로 사용한다.
- 구현 방향: `ICameraService`를 `ICameraSource`와 `MultiCameraCoordinator` 구조로 확장하고, 프레임에는 width/height/stride/pixel format/timestamp/camera id를 포함한다.
- 확인 필요: DC-T3145G/R이 기존 `MVSDK_Net`/`IMV` 샘플과 호환되는지, 6대 동시 트리거가 필요한지, Global Shutter 2대를 어떤 ViewType에 배치할지 확인한다.

## ADR-007: 학습 중 검사를 앱에서 막지 않고 SDK로 넘긴다

- 날짜: 2026-08-24
- 상태: 적용
- 배경: AI 담당이 학습 도중의 검사를 OpenCV 기반 간이 검사로 대체하겠다며, 검사 요청 JSON에 학습 진행 여부를 실어 달라고 요청했다.
- 결정: 학습 중 검사 차단을 해제하고, 요청 JSON에 `trainingRunning`(0/1)을 추가해 SDK가 경로를 고르게 한다. **앱은 이 값을 실어 보내는 것 외에 학습 여부로 아무것도 다르게 하지 않는다.**
- 이유: 앱이 막으면 `trainingRunning`이 `1`로 나가는 경우가 아예 생기지 않아 간이 검사가 동작할 자리가 없다. 학습 프로세스를 띄우고 관리하는 쪽이 앱이므로 이 상태를 가장 정확히 아는 것도 앱이다.
- 변경 전: 학습 중 검사는 사진을 찍기 전에 `InspectionResult.Error`로 끝나고 이력에 ERROR로 저장됐다. 검사 버튼 자체는 막지 않았다(`CanRunInspection`에 학습 조건 없음).
- 변경 후: 캡처, 결과 파싱, 판정 기준, 이력 저장을 학습 중이든 아니든 똑같이 수행한다. 경고 이벤트도, 별도 표시도, 별도 임계값도 두지 않는다. `RunInspection`의 학습 관련 분기는 통째로 삭제했다.
- 대안 검토: 이력에 간이 검사 여부를 남기는 방안(`EventSeverity.Warning` 이벤트, 결과 JSON `inspectionMode` 필드)을 검토했으나 2026-08-24 사용자 결정으로 채택하지 않았다. 값 하나만 다르고 나머지는 같은 검사로 본다.
- 유지: `Warmup`의 학습 중 건너뛰기는 그대로 둔다. 깨우기는 첫 검사를 빠르게 하려는 것이라 학습 중에 할 이유가 없다.
- 안전성 근거: 학습 종료 후 VLAD 재초기화 구간은 `VladRuntimeLifecycleService.OperationSyncRoot` 잠금이 막는다. 그 구간의 검사 호출은 잠금 앞에서 대기했다가 재초기화 후에 들어가므로 준비되지 않은 SDK를 부르지 않는다.
- 따라오는 조건: 앱이 두 경로를 구분하지 않으므로 **SDK가 두 경로의 결과를 같은 규칙·같은 척도로 채워야 한다.** 이것이 `Q-039`·`Q-040`의 확인 사유다.
- 사양: `20-during-development/vision/VLAD_HD_Inference_Mat_학습중상태전달-2026-08-24.md`
- 확인 필요: `Q-039`(간이 검사 score 척도), `Q-040`(간이 검사 measurements) 회신 전까지 `O-041`은 미완결이다.

## ADR-008: 판정은 AI 판정을 그대로 따르고, 앱은 결과를 되짚지 않는다

- 날짜: 2026-09-07
- 상태: 적용
- 배경: SDK가 결과 버퍼를 채우지 않고 그대로 돌려주는 경우가 확인됐다. 2026-09-04 로그에서 `[HD_INFERENCE_REQUEST]`와 `[HD_INFERENCE_RESULT]`의 JSON이 글자 하나까지 같고 Length도 203으로 같다. 이때 앱이 넣어 보낸 `viewJudge` 0이 그대로 돌아오고, 그것이 합격으로 읽힌다. DB에서 `scoreThreshold` 95인 14건이 모두 `score` 0이면서 PASS로 남아 있다.
- 결정: **앱은 지금처럼 AI 판정을 그대로 따른다.** 「요청과 결과가 같으면 검사 실패로 본다」는 방어를 넣지 않는다.
- 이유: 판정 주체는 AI다. 앱이 결과를 되짚어 판정을 뒤집기 시작하면 판정 규칙이 두 곳으로 갈라진다. 결과를 채우지 않는 것은 SDK 쪽에서 고칠 일이고 AI 담당에게 이미 전달했다.
- 검토했으나 채택하지 않은 것: 요청 JSON과 결과 JSON을 비교해 같으면 `InspectionResult.Error`로 돌리는 방안. 2026-09-07 사용자 결정으로 넣지 않는다.
- 따라오는 위험: SDK가 결과를 채우지 않는 동안에는 **검사가 실제로 수행되지 않은 건이 PASS로 남는다.** 앱은 이것을 걸러내지 않으므로, 현장에서 이력의 PASS를 볼 때 `Score`가 `0.00`인 줄은 검사가 돌지 않았을 수 있다고 봐야 한다.
- 관련: `O-042`, `Q-041`. 판정 코드는 `JudgmentService.Judge`의 `HasAuthoritativeJudgment` 경로다.

