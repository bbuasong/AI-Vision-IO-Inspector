# 미확정 질문

## 2026-08-11 기준

상태는 `완료`, `부분확정`, `외부확인필요`, `범위조정필요`로 관리합니다. 외부확인필요 항목은 `open-items.md`의 ID와 연결해 내부 준비 작업을 같이 추적합니다.

이전 기준일은 2026-05-31이었습니다. 2026-08-11 갱신에서 그 사이 확정된 항목의 상태를 반영하고, `open-items.md` 재번호 이후 어긋나 있던 `O-` 참조를 정정했습니다.

| ID | 질문 | 배경/근거 | 필요 답변자 | 상태 | 연결 항목 |
| --- | --- | --- | --- | --- | --- |
| Q-001 | Application 개발 범위는 어디까지인가? | C# 앱/DB/UI/연동 뼈대까지 구현 중 | PM/리더 | 부분확정 | `O-001`, `O-006`, `O-007` |
| Q-002 | AI 모델 학습/개발은 담당 범위에서 제외되는가? | 결과 JSON 계약은 확정됐지만 실제 AI 모델/학습/최종 export는 AI 담당 영역 | PM/AI 담당 | 부분확정 | `O-001`, `O-006` |
| Q-003 | 카메라 SDK 연동도 Application 개발 범위에 포함되는가? | RTSP 방식으로 확정됐습니다. 라이브는 LibVLC, 캡처는 VLAD RTSP callback을 사용합니다 | PM/장비 담당 | 완료 | `O-004`, `O-032` |
| Q-010 | 개발 프레임워크는 C# WPF인가, WinForms인가? | UI 구조와 구현 방식 결정 | 개발 리더 | 완료 | WPF |
| Q-011 | .NET 버전과 Visual Studio 버전은 무엇인가? | 빌드 환경 구성 필요 | 개발 리더 | 완료 | Visual Studio 2022, WPF, .NET Framework 4.7.2, x64 전용 |
| Q-012 | 현재 소스는 화면만 구현된 상태인가, DB/카메라/AI 연동까지 일부 구현됐는가? | 작업 범위 산정 필요 | 개발 리더 | 완료 | DB/UI/RTSP/VLAD 연동 구현 |
| Q-013 | DBMS와 접속 방식은 무엇인가? | Repository/배포 구조 결정 | DB 담당 | 완료 | SQLite `DataBase.db` |
| Q-020 | AI 모델은 DLL, SDK, EXE, Python, REST API 중 어떤 방식으로 호출하는가? | `VLAD_SDK.dll` P/Invoke로 확정 | AI 담당 | 완료 | `O-006` |
| Q-021 | AI 추론 입력/출력 스키마는 확정됐는가? | `VLAD_HD_Inference_Mat` 3인자 in-place JSON, 8192-byte UTF-8 고정 버퍼로 계약 확정. 실제 DLL 실호출 검증이 남음 | AI 담당 | 부분확정 | `O-016`, `O-017` |
| Q-022 | VLAD SDK의 Register/Unregister/Class 기능을 Application에서 직접 호출해야 하는가? | C#이 시작 시 `VLAD_Custom_Registration`을 직접 호출하는 구조로 구현됨 | AI 담당 | 부분확정 | `O-026` |
| Q-023 | 실제 카메라 SDK 문서와 설치 DLL은 어디에 있는가? | IMV/MVSDK 직접 연결이 아니라 RTSP 수신 방식으로 전환됨 | 장비 담당 | 완료 | `O-004` |
| Q-024 | 촬영 뷰는 카메라 6대로 확정인가? | Top/Front/Back/Left/Right/Thickness UI/설정 구조 영향 | 장비 담당 | 완료 | 6대 확정, `CFG\Config.json`의 `CAM0`~`CAM5` |
| Q-025 | 라벨/품번 입력 방식은 무엇인가? | Epson ES-C320W 스캐너 OCR로 확정. 미등록 품번은 등록 화면으로 전환 | 고객/IT | 완료 | `O-015` |
| Q-030 | 측정부1~측정부5 독립 방식은 최종 요구사항인가? | 길이/너비/높이/두께 고정 세트가 아니라 독립 측정부 구조로 변경됨 | 고객/PM | 완료 | 최대 5개 독립 측정부 구조 구현, 현장 검증은 `O-014`, `O-034` |
| Q-031 | 각 측정부의 허용오차 기준은 어떻게 관리하는가? | 기준값/허용값/단위 구조 구현됨. 신규 계약에서는 AI가 측정부별 `judge`를 반환하므로 최종 판정 주체 확인 필요 | 고객/품질 담당 | 부분확정 | `O-007`, `O-014` |
| Q-032 | 엑셀 일괄등록은 1차 범위에 포함되는가? | 현재 CSV 다중등록은 구현됨 | PM/고객 | 범위조정필요 | `O-013` |
| Q-033 | 통계정보는 1차 필수 범위인가, 추가 범위인가? | 기본 통계 화면과 기간 필터 구현됨 | PM/고객 | 부분확정 | `O-012` |
| Q-034 | 검사 이력 저장 정책은 1개월 기준인가, 저장공간 기준인가? | 현재 보관기간/저장공간 기준 삭제 구조 있음 | PM/고객 | 부분확정 | `O-009` |
| Q-035 | NG 이미지는 일반 OK 이미지보다 더 오래 보존해야 하는가? | 이미지 저장공간 정책에 영향 | 고객/품질 담당 | 외부확인필요 | `O-009` |
| Q-036 | 평택물류센터 설치 전까지 완료해야 하는 산출물은 무엇인가? | 납품 기준 확인 | PM | 외부확인필요 | `O-010` |
| Q-037 | 고장 카메라가 있을 때 검사를 계속할 것인가? | 정상 채널만 처리할지, 실패 채널을 ERROR로 최종 판정에 반영할지 정책 미확정 | PM/고객 | 외부확인필요 | `O-030`, `O-033` |
| Q-038 | 결과 이미지에 W/D/H와 측정값을 합성해 표시할 것인가? | `dimensions`와 `measurements`는 파싱되지만 결과 PNG 합성 구현은 없음 | PM/고객 | 외부확인필요 | `O-027`, `O-028` |

## 우선 답변 필요 항목

1. 새 export(`VLAD_HD_Inference_Mat`, `VLAD_Search_Mat`, `VLAD_HD_ImageMerge`)가 포함된 `VLAD_SDK.dll` 제공 시점 (`O-017`, `O-018`, `O-029`)
2. `RuntimeData\Models\VLAD\Ex_Weight`가 VLAD_SDK가 직접 읽는 최종 추론 모델 구조인지 확인 (`O-001`)
3. AI DLL 내부 측정값이 mm 단위로 반환되는지, pixel-to-mm 보정 담당 범위는 어디인지 (`O-007`)
4. 고장 카메라 포함 시 검사 진행/최종 판정 정책 (`O-033`)
5. OK/NG 이미지 보존 기간과 디스크 한계 정책 (`O-009`)
6. 배포 PC의 CUDA/cuDNN/VC++ Runtime 설치 기준 (`O-002`)

## 2026-08-11 정정 내역

`open-items.md` 재번호 이후 어긋나 있던 참조를 다음과 같이 정정했습니다.

| 질문 | 이전 참조 | 정정 후 | 사유 |
| --- | --- | --- | --- |
| Q-003, Q-023 | `O-001` | `O-004` | 카메라 연동은 RTSP 수신 항목에 해당 |
| Q-021 | `O-006`, `O-024`, `O-025` | `O-016`, `O-017` | 현재 MAT JSON 계약 검증 항목으로 연결 |
| Q-022 | `O-006` | `O-026` | 두 VladId 등록 검증 항목에 해당 |
| Q-025 | `O-009` | `O-015` | `O-009`는 History 보존 정책이며 OCR과 무관 |
| Q-031 | `O-004` | `O-007`, `O-014` | `O-004`는 RTSP/NVR 항목 |
| Q-032 | `O-012` | `O-013` | `O-012`는 통계 UI |
| Q-033 | `O-011` | `O-012` | `O-011`은 Git 대용량 파일 |
| Q-034, Q-035 | `O-008` | `O-009` | `O-008`은 기준 이미지 정책 |
