# 30. 잔건과 향후 고려사항

기준일: 2026-08-11

아직 끝나지 않은 일과, 확정되지 않아 앞으로 결정해야 할 사항을 모았습니다.

| 파일 | 내용 |
| --- | --- |
| `open-items.md` | 날짜별 미구현/미비/검증필요 항목. `O-` ID로 관리 |
| `questions.md` | 미확정 질문. `Q-` ID로 관리하며 답변자와 연결 항목을 함께 기록 |
| `task-board.md` | 작업 단위 현재 상태와 완료 기준. `T-` ID로 관리 |

세 문서는 서로 연결됩니다. `task-board.md`의 각 작업은 미해결 사유를 `O-` ID로 가리키고, `questions.md`의 질문도 `O-` ID로 내부 준비 작업과 연결됩니다.

## 지금 막고 있는 것

현재 남은 작업의 대부분은 코드가 아니라 **외부 입력 대기**입니다.

### 1. 새 export가 포함된 `VLAD_SDK.dll` 대기

관리 코드는 이미 구현돼 있고 DLL 교체 후 실호출 검증만 남았습니다.

| 항목 | 관련 ID |
| --- | --- |
| `VLAD_HD_Inference_Mat` 실호출 | `O-016`, `O-017` |
| `VLAD_Search_Mat` / `VLAD_Search_ResultData` 유사도 검색 | `O-018`, `T-210` |
| `VLAD_HD_ImageMerge` 이미지 병합 | `O-029`, `T-200` |
| 같은 프로세스의 두 VladId 등록 | `O-026` |

### 2. 현장 실기 검증 대기

| 항목 | 관련 ID |
| --- | --- |
| 6개 RTSP URL 단독 접속 로그 | `O-032` |
| 고장 카메라 포함 시 검사 진행 | `O-033` |
| Thickness 측정부/coordinate 기준 이미지 매핑 | `O-034` |
| 장시간 스트리밍 안정성 | `O-004`, `O-003` |

### 3. AI 담당자 확인 대기

| 항목 | 관련 ID |
| --- | --- |
| `Ex_Weight`가 최종 추론 모델 구조인지 | `O-001` |
| AI 측정값이 mm 단위로 반환되는지, pixel-to-mm 담당 범위 | `O-007` |

### 4. 고객/PM 정책 결정 대기

| 항목 | 관련 ID |
| --- | --- |
| 고장 카메라 포함 검사 정책 | `Q-037`, `O-030`, `O-033` |
| 결과 이미지에 W/D/H·측정값 합성 여부 | `Q-038`, `O-027`, `O-028` |
| OK/NG 이미지 보존 기간 | `Q-034`, `Q-035`, `O-009` |
| 엑셀 일괄등록 범위 | `Q-032`, `O-013` |

### 5. 배포 환경

| 항목 | 관련 ID |
| --- | --- |
| CUDA/cuDNN/VC++ Runtime 배치 또는 설치 | `O-002` |
| 클린 PC 배포 패키지 검증 | `O-010` |
| Git 대용량 파일 관리 방식 | `O-011` |

## 다음 처리 순서

`task-board.md`의 `다음 처리 순서` 절을 기준으로 진행합니다. 요약하면 다음과 같습니다.

1. `O-032` RTSP 단독 접속 로그 판독
2. `O-033` 고장 카메라 시나리오 검증
3. `O-034` 측정부 등록 품목 검사 검증
4. `O-029`, `O-018` 새 DLL 교체 후 실호출 검증
5. `O-001`, `O-002` 모델 구조와 런타임 배치 확정
6. `O-035`, `O-036` 용어 정리와 아이콘 적용 결정

## 아직 문서에 없는 향후 고려사항

2026-08-11 검토에서 확인됐으나 위 `O-`/`Q-` ID로 등록되지 않은 항목입니다. 진행 여부가 정해지면 `open-items.md`에 정식 등록합니다.

| 항목 | 내용 |
| --- | --- |
| 자동 검증 수단 부재 | 단위 테스트 프로젝트와 CI 워크플로가 없습니다. `.github/workflows`는 비어 있고 테스트 프로젝트는 0개입니다. `Domain`/`Application`은 순수 로직이라 지금도 테스트 가능합니다 |
| Git 작업 트리 불일치 | 현재 개발 소스 `Codes\AI-Vision IO Inspector\`가 untracked이고, 이전 폴더들이 삭제 상태로 남아 있습니다. 폴더 재구성을 Git에 반영할지 결정이 필요합니다 |
| `changelog.md` 번호 중복 | `182`, `183`이 각각 두 번 사용됐습니다(2026-07-20자, 2026-07-23자). 기존 참조가 깨지지 않도록 정리 방식을 정해야 합니다 |
