# 작업 보드

## 2026-08-11 상태 기준

상세 미구현/미비 항목은 `open-items.md`에서 날짜별로 추적합니다. 이 문서는 작업 단위의 현재 상태와 완료 기준을 관리합니다.

이전 기준일은 2026-05-31이었습니다. 2026-08-11 갱신에서 아래 항목의 상태와 `O-` 참조를 현재 `open-items.md` 기준으로 정정했습니다.

| ID | 작업 | 완료 기준 | 우선순위 | 상태 | 미비/다음 액션 |
| --- | --- | --- | --- | --- | --- |
| T-001 | 현재 소스코드 위치 및 접근 권한 확인 | 저장소/폴더 위치와 접근 방법 기록 | 상 | 완료 | `Codes\AI-Vision IO Inspector` (2026-08-11 경로 정정) |
| T-002 | 개발 환경 확인 | Visual Studio, .NET 버전, WPF/WinForms 여부 기록 | 상 | 완료 | Visual Studio 2022, WPF, .NET Framework 4.7.2, x64 |
| T-003 | 빌드 확인 | 로컬 빌드 결과 기록 | 상 | 완료 | 2026-08-05 x64 Debug 전체 빌드 경고 0/오류 0 |
| T-004 | 구현 화면 현황 파악 | 메인/등록/조회/이력/통계/옵션 상태 기록 | 상 | 완료 | 옵션 탭까지 추가 |
| T-005 | DB 구조 확인 | 부품, 측정부, 검사 이력 테이블 구조 확보 | 상 | 완료 | SQLite `DB\DataBase.db`, `PartList_*` / `History_*` 계열 |
| T-006 | AI 연동 방식 확인 | 호출 방식과 입출력 스키마 확보 | 상 | 완료-DLL검증대기 | `VLAD_HD_Inference_Mat` 3인자 in-place JSON 계약 확정. 실호출 검증은 `O-016`, `O-017` |
| T-007 | 카메라 SDK 확인 | SDK 문서, 샘플 코드, 촬영 이미지 포맷 확보 | 상 | 완료 | RTSP 방식 확정. 라이브는 LibVLC, 캡처는 VLAD RTSP callback |
| T-008 | 라벨/품번 입력 방식 확정 | 스캐너/QR/OCR/수동 입력 중 최종 방식 확인 | 상 | 완료-검증필요 | Epson ES-C320W OCR 확정. 정확도 검증은 `O-015` |
| T-009 | 측정부 구조 구현 | 동적 측정부 구조가 DB/UI/CSV에 반영 | 상 | 완료 | 측정부 1~5 독립 구조, `IndexNo` 기준 |
| T-010 | 부품 기준정보 CRUD | 등록/조회/수정/삭제 가능 | 상 | 완료 | 단일/다중 등록 포함 |
| T-020 | 기준 이미지 6방향 관리 | Top/Front/Back/Left/Right/Thickness 이미지 저장/표시 | 상 | 완료 | 위치별 유니크 저장/교체/백업 |
| T-030 | DB 검색/조회 화면 구현 | 검색, 추천어, 선택 상세 확인 가능 | 상 | 완료 | DataStore 캐시/추천 10개 제한 |
| T-040 | 메인 검사 흐름 구현 | 입력→조회→촬영→추론→측정→판정→저장 동작 | 상 | 완료-실기검증필요 | `InspectionWorkflowService.RunInspection()` 구현. 실기 검증은 `O-033`, `O-034` |
| T-050 | 카메라 예외 처리 | 연결 실패, 촬영 실패 메시지와 로그 확인 | 상 | 부분완료-정책미확정 | 채널별 실패 메시지는 있으나 최종 판정 정책 미확정. `O-030`, `O-033` |
| T-060 | AI 추론 어댑터 구현 | 추론 결과 수신 및 판정 로직 연결 | 상 | 완료-DLL검증대기 | `VladVisionInferenceEngine`, `VladInferenceResultParser` 구현. 실호출은 `O-017` |
| T-070 | 측정값 비교 로직 | 기준값/허용오차/측정값 비교 결과 저장 | 상 | 완료 | 신규 결과는 AI `viewJudge`/측정부 `judge` 우선, 구형 결과만 로컬 비교 fallback |
| T-080 | 다중품목 CSV 등록 | CSV 불러오기/전체 내보내기 가능 | 중상 | 완료 | 운영 포맷 최종안은 `O-013` |
| T-090 | NG 불일치 부위 표시 | NG 위치와 사유를 화면/이력에서 확인 가능 | 상 | 부분완료 | 결과 PNG에 W/D/H·측정값 합성은 미구현. `O-027`, `O-028` |
| T-100 | 검사 이력 저장/조회 | 결과, 이미지, 측정값, 이벤트 조회 가능 | 상 | 완료 | 검색 후 CSV 저장 포함 |
| T-110 | 이벤트 로그 관리 | 카메라/AI/DB 이벤트 저장 및 조회 가능 | 중 | 부분완료-검증필요 | 실제 SDK/AI 예외 로그 매핑 필요 |
| T-120 | 통계 화면 구현 | 등록 부품수, 검사실적, OK/NG, 평균 검사시간 표시 | 중 | 부분완료-검증필요 | 기간 필터 구현. 운영 KPI 기준은 `O-012` |
| T-130 | WPF MVVM 솔루션 구성 | .NET Framework 4.7.2 WPF x64 빌드 가능 | 상 | 완료 | 2026-05-29 최초 생성, 2026-06-22 net472/x64 전환, 2026-08 `Codes\` 이관 |
| T-140 | 카메라 옵션 UI | 6대 카메라 설정/상태 표시 | 상 | 완료 | `CFG\Config.json`의 `CUSTOM.HD.CAMS` 6채널 |
| T-150 | 카메라 촬영 Worker | ViewType별 촬영 요청 Worker 구성 | 상 | 완료 | `VLAD_Ops_RTSP.TryCloneLatestFrames()` 기준 |
| T-160 | 실제 카메라 연결 | 실제 프레임 수신 | 상 | 완료-현장검증필요 | RTSP 6채널 구성 완료. 단독 접속 로그 검증은 `O-032`, 장시간 안정성은 `O-004` |
| T-170 | 연속 영상 미리보기 | 6대 최신 프레임을 UI에 지속 표시 | 상 | 완료 | `RtspVideoHost` LibVLC 직접 렌더링 |
| T-180 | 카메라 보정 | pixel-to-mm 변환 기준 확보 | 상 | 미적용-AI담당 | 앱은 단위 변환을 하지 않고 AI 반환 mm 값을 그대로 사용. `O-007` |
| T-190 | VLAD 실제 추론 연결 | 모델 등록/워밍업/추론/해제 동작 | 상 | 완료-DLL검증대기 | `VLAD_Custom_Registration` 호출 구조 확인. 최종 모델 구조는 `O-001` |
| T-200 | 기준 이미지 병합 | 품번 이름의 6장 합성 이미지 생성/삭제 | 중 | 구현완료-DLL대기 | `VLAD_HD_ImageMerge` 연결 완료. 현재 배포 DLL에 export 없음. `O-029` |
| T-210 | 유사도 검색 | 기준 이상 후보 최대 3개 표시 | 중 | 구현완료-DLL대기 | `VLAD_Search_Mat`/`VLAD_Search_ResultData`만 호출. `O-018` |
| T-220 | OCR 미등록 품번 등록 전환 | 미등록 품번을 등록 화면으로 연결 | 중 | 완료 | 2026-08-10 실동작 확인 |
| T-230 | 이력 보존/삭제 정책 | 보존기간·여유공간 기준 자동 삭제 | 중 | 구현완료-검증필요 | `InspectionDataRetentionService`. 임계치 상황 검증은 `O-009` |

## 다음 처리 순서

`open-items.md`의 2026-08-10 현장 검증 잔여 항목을 우선합니다.

1. `O-032` `cam1.txt`~`cam6.txt`를 채널별로 판독해 6개 RTSP URL의 단독 접속 상태를 확정합니다.
2. `O-033` 일부 카메라 실패 상태를 의도적으로 만들어 정상 채널 처리, 실패 채널 로그, 최종 판정, stale snapshot 재사용 여부를 확인합니다.
3. `O-034` 측정부가 등록된 실제 품목으로 Thickness 표시값, coordinate 기준 이미지, `IndexNo` 매핑을 확인합니다.
4. `O-029`, `O-018` 새 export가 포함된 `VLAD_SDK.dll`로 교체한 뒤 이미지 병합과 유사도 검색을 실호출 검증합니다.
5. `O-001`, `O-002` 최종 추론 모델 구조와 CUDA/cuDNN/VC++ Runtime 배치를 AI 담당자/배포 PC 기준으로 확정합니다.
6. `O-035`, `O-036` 과거 Result/Data 용어 정리와 프로그램 아이콘 적용 여부를 결정합니다.
