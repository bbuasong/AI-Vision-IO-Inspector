# Work Log - 2026-07-15

## 완료

- 프로그램 시작 시 옵션 카메라 상태가 실제 연결 확인 결과, 메시지, 확인시각으로 갱신되도록 보완했다.
- 기준이미지 더블클릭 팝업을 추가했다. 팝업은 단일 인스턴스로 유지되고 Search DB 선택 변경 시 현재 부품으로 갱신된다.
- 측정부 coordinate 이미지가 존재하는 경우 Thickness 기준이미지에 적용했다.
- `Config.json`에 검사 Score와 단일품목 유사도 설정을 저장하고, 검사 워크플로우의 Score 판정 기준으로 반영했다.
- 검사 완료 이미지에 Score와 W/H/D 표시 영역을 추가했다. W/H/D는 현재 DLL 결과 미제공으로 대기 상태다.
- 유사도 검색 UI와 결과 초기화 UI를 추가했다. 전용 VLAD Search export가 없어 실제 검색 호출은 보류했다.
- 불필요한 `detectData == IntPtr.Zero` 이후 throw 문을 제거했다.

## 검증

- `.NET Framework 4.7.2`, `x64`, Debug 전체 솔루션을 분리 출력 경로에서 빌드한다.
- 기준이미지 팝업, Config 저장/재실행 로드, 실제 6채널, VLAD SDK 결과 계약은 장비/AI 담당자 환경에서 추가 검증이 필요하다.
