<Window x:Class="ExternalTraining.Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="External Training Program Sample" Height="720" Width="980"
        MinHeight="640" MinWidth="900" FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="132" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="4" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="12" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="4,0,0,6" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="External Training Program" FontSize="20" FontWeight="SemiBold" />
            <TextBlock DockPanel.Dock="Right" Text="버튼 클릭 → StandardOutput 전송" Foreground="#5B6472" />
        </DockPanel>

        <GroupBox Grid.Row="1" Header="WPF에서 전달받은 Argument">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="88" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="88" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Job ID" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding JobId, Mode=OneWay}" IsReadOnly="True" />
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Log" Margin="12,0,0,0" />
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding LogPath, Mode=OneWay}" IsReadOnly="True" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Input" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding InputPath, Mode=OneWay}" IsReadOnly="True" />
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Output" Margin="12,0,0,0" />
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding OutputPath, Mode=OneWay}" IsReadOnly="True" />
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="메시지 종류별 전송 버튼">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="정상 흐름: Start → Progress → Done" />
                    <WrapPanel>
                        <Button Content="START 0" Click="Start_Click" />
                        <Button Content="PROGRESS 10" Click="Progress10_Click" />
                        <Button Content="PROGRESS 30" Click="Progress30_Click" />
                        <Button Content="PROGRESS 60" Click="Progress60_Click" />
                        <Button Content="PROGRESS 90" Click="Progress90_Click" />
                        <Button Content="DONE 100" Click="Done_Click" />
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,10">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="정보 / 경고 / 진단" />
                    <WrapPanel>
                        <Button Content="LOG 이미지 로딩" Click="LogImage_Click" />
                        <Button Content="WARN W001" Click="Warn_Click" />
                        <Button Content="STDERR 진단" Click="Stderr_Click" />
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,0,10">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="오류" />
                    <WrapPanel>
                        <Button Content="ERROR E001" Click="ErrorInput_Click" />
                        <Button Content="ERROR E004" Click="ErrorRuntime_Click" />
                    </WrapPanel>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="2">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="취소 / 비정상 종료" />
                    <WrapPanel>
                        <Button Content="CANCELED" Click="Canceled_Click" />
                        <Button Content="메시지 없이 종료" Click="CloseNoMessage_Click" />
                    </WrapPanel>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="이 프로그램에서 보낸 메시지">
            <DataGrid ItemsSource="{Binding SentMessages}" AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="105" />
                    <DataGridTextColumn Header="Stream" Binding="{Binding Stream}" Width="85" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="110" />
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="90" />
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
